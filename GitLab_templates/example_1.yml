# (c) DevOpsHQ, 2020

# --- Example template 1: existing project ----------------------------

include: https://gitlab.<your_company>.com/<your_project>/-/raw/<branch>/<path>/existing_project.yml

variables:
  PROJECT_LANG: python
